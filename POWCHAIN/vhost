nano /etc/nginx/sites-available/bbqfinance



nano /etc/nginx/sites-available/bbqfinance

server {
    listen 80;
    server_name bbqfinance.fun www.bbqfinance.fun;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name bbqfinance.fun www.bbqfinance.fun;

    ssl_certificate /etc/letsencrypt/live/bbqfinance.fun/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/bbqfinance.fun/privkey.pem;

    root /var/www/bbqfinance;
    index index.html;

    # FRONTEND
    location / {
        try_files $uri /index.html;
    }

    # API POWCHAIN
    location /stats        { proxy_pass http://127.0.0.1:3000/stats; }
    location /chain        { proxy_pass http://127.0.0.1:3000/chain; }
    location /explorer     { proxy_pass http://127.0.0.1:3000/explorer; }
    location /lp           { proxy_pass http://127.0.0.1:3000/lp; }
    location /tx           { proxy_pass http://127.0.0.1:3000/tx; }
    location /wallet       { proxy_pass http://127.0.0.1:3000/wallet; }
    location /bridge       { proxy_pass http://127.0.0.1:3000/bridge; }

    # WebSocket POWCHAIN
    location /ws {
        proxy_pass http://127.0.0.1:7001/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
    }
}







nano /etc/nginx/nginx.conf





nginx -t
systemctl reload nginx




curl http://127.0.0.1:3000/stats
curl http://127.0.0.1:3000/chain
curl http://127.0.0.1:3000/explorer?depth=3



curl -k https://bbqfinance.fun/stats
curl -k https://bbqfinance.fun/chain
curl -k https://bbqfinance.fun/explorer?depth=3



nginx -t

curl -k https://bbqfinance.fun/stats









