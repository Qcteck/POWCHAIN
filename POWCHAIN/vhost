
sudo ss -ltnp | grep ':8080'

sudo lsof -i :8080




npm i tweetnacl
node -e "const nacl=require('tweetnacl');const k=nacl.sign.keyPair();console.log('PUB',Buffer.from(k.publicKey).toString('hex'));console.log('PRIV',Buffer.from(k.secretKey).toString('hex'));"




sudo apt update
sudo apt install -y clang
clang --target=wasm32 -O3 -nostdlib -Wl,--no-entry -Wl,--export-all -o qplus.wasm qplus.c


npm i express tweetnacl
node signer.js