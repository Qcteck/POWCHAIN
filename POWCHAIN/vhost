# 1) Éditer la conf
sudo nano /etc/nginx/sites-available/bbqfinance.conf
# colle le bloc ci-dessus, sauvegarde (Ctrl+O, Entrée, Ctrl+X)

# 2) Vérifier la syntaxe nginx
sudo nginx -t

# 3) Si "syntax is ok" → recharger
sudo systemctl reload nginx