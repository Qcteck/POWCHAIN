sudo ls /etc/letsencrypt/live/bbqfinance.fun/
sudo certbot --nginx -d bbqfinance.fun -d www.bbqfinance.fun

server {
    listen 80;
    server_name bbqfinance.fun www.bbqfinance.fun;
    return 301 https://bbqfinance.fun$request_uri;
}



sudo ls /etc/letsencrypt/live/bbqfinance.fun/


pm2 ls
# tu dois voir ton serveur powchain online (port 3000)
sudo netstat -tulnp | grep 7001
# WS doit être en LISTEN sur 7001