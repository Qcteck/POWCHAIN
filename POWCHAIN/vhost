# Tester toute la config Nginx
sudo nginx -t

# Recharger Nginx (si le test est OK)
sudo systemctl reload nginx

# Voir si Nginx tourne bien
sudo systemctl status nginx

# Voir ce qui écoute sur le port 80
sudo ss -tlnp | grep ':80'