bash -c "$(cat <<'BASH'
set -euo pipefail
HTML="/var/www/wolverine/index.html"
TS=$(date +%Y%m%d_%H%M%S)

echo "=== USDC HARD FIX (PHANTOM-COMPATIBLE) ==="
cp "$HTML" "$HTML.usdc_hardfix_$TS"

# Remplacer TOUT le bloc USDC par une version robuste
perl -0777 -i -pe '
s@// USDC[\s\S]*?u\.textContent=.*?;@// USDC (robuste)\nconst allAccs=await conn.getParsedTokenAccountsByOwner(pub,{programId:new solanaWeb3.PublicKey(\"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA\")});\nlet tot=0;\n(allAccs.value||[]).forEach(x=>{\n const info=x.account.data.parsed.info;\n if(info.mint===\"EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1\"){\n  const ta=info.tokenAmount;\n  tot+=Number(ta.amount)/Math.pow(10,ta.decimals);\n }\n});\nu.textContent=tot.toFixed(2);@g
' "$HTML"

echo "=== DONE ==="
echo "➡️ HARD REFRESH NAVIGATEUR (CTRL+SHIFT+R)"
BASH
)"