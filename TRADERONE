URL="https://fireinners.online/wolverine/traderone/api/status"
KEY=$(grep -oP 'http_x_api_key != "\K[^"]+' /etc/nginx/sites-enabled/fireinners.online-ssl | head -n1)

echo "=== PUBLIC AVEC clé (200 attendu) ==="
curl -sS -i -X POST "$URL" -H "content-type: application/json" -H "X-API-KEY: $KEY" --data '{}' | head -n 12

echo
echo "=== PUBLIC SANS clé (403 attendu) ==="
curl -sS -i -X POST "$URL" -H "content-type: application/json" --data '{}' | head -n 12