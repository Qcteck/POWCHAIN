KEY=$(grep -oP 'http_x_api_key != "\K[^"]+' /etc/nginx/sites-enabled/fireinners.online-ssl | head -n1)
curl -sS -i -X POST "https://fireinners.online/wolverine/traderone/api/status" \
  -H "content-type: application/json" \
  -H "X-API-KEY: $KEY" \
  --data '{}' | head -n 25 