# désactive le pager pour cette session
export SYSTEMD_PAGER=cat
export PAGER=cat
export LESS=FRX

# exemples sans pager
journalctl -u traderone.service -n 80 --no-pager
journalctl -u traderone-watchdog.service -n 80 --no-pager
nginx -T 2>/dev/null | head -n 80