BASE="https://bbtrader-jup.paycashnostory.workers.dev"

echo "== / =="
curl -sS -D- -o /dev/null "$BASE/" | head -n 15
curl -sS "$BASE/" | head -c 120; echo

echo "== /swap =="
curl -sS -D- -o /dev/null "$BASE/swap" | head -n 15
curl -sS "$BASE/swap" | head -c 120; echo

echo "== /quote =="
curl -sS -D- -o /dev/null "$BASE/quote" | head -n 15
curl -sS "$BASE/quote" | head -c 120; echo