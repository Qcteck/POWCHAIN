cat >>/etc/nginx/snippets/wolverine_traderone_stub_locations.conf <<'NGINX'

# UI calls this (absolute path). Stub for now.
location = /api/sync-deposit {
  default_type application/json;
  return 200 '{"ok":true,"message":"stubbed sync-deposit"}';
}
NGINX

nginx -t && systemctl reload nginx