set -euo pipefail
ROOT="/var/www/fireinners-test"

echo "=== Scan images (safe) dans $ROOT ==="
find "$ROOT" -maxdepth 1 -type f \( -iname "*.png" -o -iname "*.jpg" -o -iname "*.jpeg" \) \
  -printf "%f\n" | sort | head -n 200

echo
echo "=== Compte ==="
find "$ROOT" -maxdepth 1 -type f \( -iname "*.png" -o -iname "*.jpg" -o -iname "*.jpeg" \) | wc -l