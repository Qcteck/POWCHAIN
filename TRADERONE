cd /opt/traderone-opt

# (optionnel) backup
cp -v package.json package-lock.json /root/ 2>/dev/null || true

# si tu veux utiliser le SDK LI.FI (optionnel)
npm i @lifi/sdk

# sinon, si tu fais "API direct" (recommandé), tu n'as rien à installer (fetch existe déjà en Node 20)