set -euo pipefail
APPJS="/var/www/fireinners/wolverine/app.js"
TS="$(date +%s)"
cp -a "$APPJS" "$APPJS.bak.$TS"
echo "Backup: $APPJS.bak.$TS"

# remplace toutes les occurrences mainnet-beta par un RPC qui accepte le browser
# (Ankr public RPC)
sed -i 's#https://api\.mainnet-beta\.solana\.com#https://rpc.ankr.com/solana#g' "$APPJS"

echo "DONE ✅ Ctrl+F5 sur /wolverine puis reteste START"