sudo -i

# si tmux pas installé
command -v tmux >/dev/null 2>&1 || (apt-get update -y && apt-get install -y tmux)

# créer/attacher la session "wolvfix"
tmux has-session -t wolvfix 2>/dev/null && tmux attach -t wolvfix || tmux new -s wolvfix