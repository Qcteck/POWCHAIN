bash -c "$(cat <<'BASH'
set -euo pipefail
HTML="/var/www/wolverine/index.html"
TS=$(date +%Y%m%d_%H%M%S)

echo "=== FIX USDC TOKEN-2022 (FINAL) ==="
cp "$HTML" "$HTML.usdc_token2022_fix_$TS"

perl -0777 -i -pe '
s@// USDC \(robuste\)[\s\S]*?u\.textContent=.*?;@// USDC (legacy + token-2022)\nconst USDC_MINT=\"EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1\";\nconst PROGRAMS=[\n new solanaWeb3.PublicKey(\"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA\"),\n new solanaWeb3.PublicKey(\"TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb\")\n];\nlet tot=0;\nfor(const prog of PROGRAMS){\n const accs=await conn.getParsedTokenAccountsByOwner(pub,{programId:prog});\n (accs.value||[]).forEach(x=>{\n  const info=x.account.data.parsed.info;\n  if(info.mint===USDC_MINT){\n   const ta=info.tokenAmount;\n   tot+=Number(ta.amount)/Math.pow(10,ta.decimals);\n  }\n });\n}\nu.textContent=tot.toFixed(2);@g
' "$HTML"

echo "=== DONE ==="
echo "➡️ HARD REFRESH (CTRL+SHIFT+R)"
BASH
)"