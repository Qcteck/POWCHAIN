bash <<'EOF'
set -e
echo "=== WOLVERINE • FIX WalletWrap JSX ==="

WOLV="/opt/fireinners/wolverine"
cd "$WOLV"

echo "[1/3] Rewrite App.jsx (safe wrapper fix)"

cat > src/App.jsx <<'JSX'
import WalletWrap from './components/WalletProvider'
import WalletBox from './components/WalletBox'

export default function App() {
  return (
    <WalletWrap>
      <div className="app">
        <WalletBox />
        {/* reste de ton UI EXISTANTE ici */}
      </div>
    </WalletWrap>
  )
}
JSX

echo "[2/3] Build"
npx vite build

echo "[3/3] DONE"
echo "Reload the page in Phantom browser"
EOF