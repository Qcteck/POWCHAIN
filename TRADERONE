# 1) Voir les règles actuelles (avec numéros)
sudo ufw status numbered

# 2) Supprimer TOUTES les lignes "ALLOW 3000/tcp" (v4 et v6)
# Remplace X par le numéro affiché, et répète jusqu’à ce qu’il n’y en ait plus.
sudo ufw delete X

# 3) Supprimer TOUTES les lignes "ALLOW 8484/tcp" (v4 et v6)
# Remplace Y par le numéro affiché, et répète jusqu’à ce qu’il n’y en ait plus.
sudo ufw delete Y

# 4) Vérif rapide: il doit rester 443 et ssh (22) seulement (et wg si tu l’utilises)
sudo ufw status verbose



# doit marcher (local)
curl -m 3 -I http://127.0.0.1:3000 | head
curl -m 3 -I http://127.0.0.1:8484 | head

# doit FAIL (bloqué par UFW si l’app écoute sur 0.0.0.0)
curl -m 3 -I http://72.61.79.12:3000 | head
curl -m 3 -I http://72.61.79.12:8484 | head




ss -lntp | egrep ':443|:3000|:8484|:4001'


