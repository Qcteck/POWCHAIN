// exemple (ton bundle expose Pa = bip39)
const wl = Pa.wordlists.english; // ou portuguese / japanese
const ok = Pa.validateMnemonic(mnemonic, wl);
const ent = Pa.mnemonicToEntropy(mnemonic, wl);
const m2  = Pa.entropyToMnemonic(ent, wl);