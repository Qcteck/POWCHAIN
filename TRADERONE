cat >/root/BUNDLER_PAYER_BALANCE.sh <<'SH'
#!/usr/bin/env bash
set -euo pipefail
RPC="https://lb.drpc.live/solana/Ah_pmhKMs0qntHMrHyHMTvqg8lcNcRkR8LWNEklbR4ac"
node - <<'JS'
const fs=require('fs');
const {Connection,Keypair,LAMPORTS_PER_SOL}=require('@solana/web3.js');
(async()=>{
  const rpc=process.env.RPC||process.argv[2];
  const sk=Uint8Array.from(JSON.parse(fs.readFileSync('/opt/bundler_api/wallet.json','utf8')));
  const kp=Keypair.fromSecretKey(sk);
  const c=new Connection(rpc,'confirmed');
  const lam=await c.getBalance(kp.publicKey);
  console.log("PAYER_PUBKEY:",kp.publicKey.toBase58());
  console.log("PAYER_SOL:",(lam/LAMPORTS_PER_SOL).toFixed(9));
  console.log("TIP: envoie ~0.01 SOL à PAYER_PUBKEY puis décoche PREFLIGHT_ONLY.");
})().catch(e=>{console.error(e);process.exit(1);});
JS "$RPC"
SH
chmod +x /root/BUNDLER_PAYER_BALANCE.sh
/root/BUNDLER_PAYER_BALANCE.sh