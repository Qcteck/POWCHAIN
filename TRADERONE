cd /root

# 1) dry-run (ne modifie rien)
APPLY=0 bash /root/fireinners_batch.sh

# 2) si le dry-run est clean (et/ou que tu veux appliquer le fix)
APPLY=1 bash /root/fireinners_batch.sh

# 3) vérif nginx + endpoints
nginx -t && systemctl reload nginx
curl -I https://fireinners.online/wolverine/ | head
curl -I https://fireinners.online/favicon.ico | head