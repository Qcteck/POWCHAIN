set -e

echo "=== AVANT ==="
ls -la /etc/nginx/sites-enabled | grep -i fireinners || true

echo "=== Supprime les backups de sites-enabled (pas les vrais fichiers) ==="
rm -f /etc/nginx/sites-enabled/*fireinners*.bak.* || true

echo "=== APRES ==="
ls -la /etc/nginx/sites-enabled | grep -i fireinners || true

echo "=== Vérif qu'il ne reste plus de conflits ==="
nginx -t

systemctl reload nginx