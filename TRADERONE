F=/etc/nginx/sites-enabled/fireinners.online-ssl

# backup
cp -a "$F" "/etc/nginx/sites-disabled/fix_braces_$(date +%Y%m%d_%H%M%S).fireinners.online-ssl"

# supprime les 4 accolades en trop (lignes 9-12)
sed -i '9,12d' "$F"

# test + reload
nginx -t && systemctl reload nginx




curl -I https://fireinners.online/wolverine/
curl -I https://fireinners.online/favicon.ico