sudo ufw status numbered

# Supprime les ALLOW dangereux visibles sur ton screenshot
sudo ufw --force delete 5
sudo ufw --force delete 9
sudo ufw --force delete 21
sudo ufw --force delete 23

# Verrouille en DENY (IPv4+IPv6) — même si déjà présent, on renforce
sudo ufw --force deny in 3000/tcp
sudo ufw --force deny in 8484/tcp
sudo ufw --force deny in 4001/tcp

sudo ufw reload
sudo ufw status numbered

# Vérif écoute + ports
sudo ss -lntp | egrep ':(443|3000|8484|4001)\b' || true


# depuis le VPS (local OK)
curl -m 3 -I http://127.0.0.1:3000 | head -n 1 || true
curl -m 3 -I http://127.0.0.1:8484 | head -n 1 || true

# tentative externe (devrait être bloquée) — si ton serveur répond encore ici, c’est pas fermé
curl -m 3 -I http://fireinners.online:3000 | head -n 1 || true
curl -m 3 -I http://fireinners.online:8484 | head -n 1 || true

