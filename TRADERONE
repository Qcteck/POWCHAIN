mkdir -p /root/nginx_bak_off
ls -1 /etc/nginx/sites-enabled | grep -E '\.bak' || true
mv -v /etc/nginx/sites-enabled/*.bak* /root/nginx_bak_off/ 2>/dev/null || true
nginx -t && systemctl reload nginx



for f in /etc/nginx/sites-enabled/*.bak*; do [ -e "$f" ] && mv -v "$f" "$f.off"; done
nginx -t && systemctl reload nginx