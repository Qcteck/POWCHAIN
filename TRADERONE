set -euo pipefail
APPJS="/var/www/fireinners/wolverine/app.js"
TS="$(date +%s)"
cp -a "$APPJS" "$APPJS.bak.$TS"
echo "Backup: $APPJS.bak.$TS"

# remplace les RPC hardcodés par le proxy same-origin
sed -i 's#https://api\.mainnet-beta\.solana\.com#/wolverine/traderone/api/rpc#g' "$APPJS"
sed -i 's#https://rpc\.ankr\.com/solana#/wolverine/traderone/api/rpc#g' "$APPJS"

echo "DONE ✅ Ctrl+F5 sur /wolverine puis reteste START"