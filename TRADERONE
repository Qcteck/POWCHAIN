bash -lc 'cd /root; ls -lb /root | sed -n "1,200p"; python3 - <<'"'"'PY'"'"'
import os,glob
for p in glob.glob("/root/*.sh"):
  b=os.path.basename(p)
  if len(b)>80 or any(c in b for c in ["\n","\r","\t","\x00"]):
    print("SUSPECT:",len(b),repr(b))
PY'