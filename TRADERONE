echo "== Actifs (sites-enabled) =="
grep -RIn --include='*' -E 'server_name\s+.*fireinners\.online' /etc/nginx/sites-enabled 2>/dev/null || true
echo

echo "== Conflits réellement chargés =="
nginx -T 2>&1 | grep -i "conflicting server name" || echo "OK: no conflicts ✅"